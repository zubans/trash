# Backend API URL used when building the Android app (production HTTPS).
VITE_API_URL=https://moya-usluga.ru

# Debug mode: show detailed error info (request URL, error message) in the app UI
VITE_DEBUG=true
